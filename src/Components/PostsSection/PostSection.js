import Header from '../HeaderSection/Header'
import styled from 'styled-components'

const Title = styled.h1`
  margin: 20rem, auto;
`

export default function PostSection() {

  return(
    <>
    <Header/>
    <Title>I will show the Post Feed once implemented!</Title>
    </>
  )
}